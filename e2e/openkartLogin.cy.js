describe("this test is for the Openkart from the Environmentvariables",()=>{
it("this is the testCase for the login ",()=>{
    const url = cypress.config("baseUrl");
    //const email = Cypress.config("dev.username");
    //const pass = Cypress.config("dev.password");
cy.visit(url);
//cy.get("#input-email").type(email);
//cy.get("[name='password']").type(pass);
//cy.contains("Login").click();


})



})